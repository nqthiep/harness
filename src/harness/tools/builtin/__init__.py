"""Pre-classified starter tools.

The beginner path never writes `@tool`: these carry their effect already, which is what
lets the five-line example exist without introducing the effect vocabulary (ADR-003,
"beginners consume, authors declare").
"""
